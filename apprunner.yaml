version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Using pre-built Docker container"
run:
  runtime-version: latest
  command: nginx -g 'daemon off;'
  network:
    port: 80
    env: PORT
  env:
    - name: NODE_ENV
      value: production
